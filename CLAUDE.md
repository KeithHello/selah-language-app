# Selah 项目规范

## 目标

Selah 是 Web 优先的语言学习应用，并保留 iOS 17+ 原生客户端。核心闭环是：用户用中文表达真实想法，系统生成自然英文与音频，用户完成聆听、理解、复习和开口练习。

## 当前工程边界

- `Package.swift` 当前只验证 macOS Swift Package 核心层，不等同于可运行的 iOS App。
- `Selah/` 包含 SwiftUI、SwiftData、服务与学习引擎代码。
- `SelahFlutter/lib/web/` 包含 Flutter Web 正式客户端；`SelahFlutter/web/` 包含浏览器存储、媒体适配与 PWA。现有原生 Flutter 预览使用独立入口，不能让其 Fixture 或原生 SQLite 进入 Web 产品依赖链。
- `supabase/` 包含数据库迁移、Edge Functions 和 Deno 测试。
- Web 实施依据：`docs/superpowers/plans/2026-09-05-selah-web-development.md`。主人已授权整理计划并执行 Web 开发；继续保留数据库 schema、密钥、CI 与公开部署的原有确认边界。
- 动画系统首轮只实施 10 个 SwiftUI 原生试点动作；已批准方向为「统一分层静态精灵 + SwiftUI 原生微动效」，不引入视频、Lottie、Rive 或新动画依赖；不得为了动画改动阻塞核心产品闭环。120 个完整动作、远端动画素材与更多表情素材不属于本轮范围。
- 2026-09-05 角色决策：当前阶段采用 C「短绒织物」，以 `design-system/selah/mascot/seed-v4-c-soft-plush.png` 为已选外观基准，角色规范见 `docs/superpowers/specs/2026-09-05-seed-plush-character.md`。Web 使用同一图集、运行时轮廓裁切和原生五官／微动效，身体手脚固定比例，叶根统一；独立透明 PNG 仍待导出，不得将 RGB 图集标为 RGBA。D「磨砂琥珀」仅保留为后续其他吉祥物候选，本阶段不开发。
- 2026-09-05 追加视觉要求：主人指出阶段和动作区分不足，新目标为五阶段各十动作，共 50 种独立表现。当前仅授权第一阶段十张关键姿态设计图及五阶段差异方案，待确认后再改运行代码。沿用 C 短绒身份，允许设计稿通过关节姿态、站坐重心和阶段结构扩大差异；不得将现有十动作复用五阶段记为 50 种设计已完成。
- 2026-09-05 后续确认：第一阶段十个动作已获主人认可，保留不重画。第三至第五阶段按已讨论的自然生长方向重新出静态外形图：低矮叶簇、叶片包裹花苞、偏侧绽放的立体布花；去掉长叶披肩，控制花叶比例，保持脸为视觉中心。当前授权为三阶段设计图，未授权把新外形接入运行代码。
- 2026-09-05 五阶段统一：主人认可第三至第五阶段 V3 的效果，要求第一、第二阶段按同一方式修改，再展示五阶段。以第三阶段 V3 作为材质、五官、手脚和布光母版，补单枚小折芽与低矮展开双叶两张静态外形图；允许幼年身体略小、略圆，保留成长递进。已确认的第一阶段十种动作设计与原图保留，本轮不重画十动作或改运行代码；五图整体仍待主人审阅。

## 2026-09-07 Web 服务与改善规划边界

- 主人决定维持现有服务链路：`gpt-4o-mini-transcribe` 转写、`gpt-4o-mini` 生成学习内容、`tts-1` 生成 MP3，并继续使用 Supabase Storage、现有声线与音频缓存。
- 本轮授权为保存费用调研、整理逐项改善设计和后续开发计划，并同步路线图；不包含产品代码实施、供应商／模型替换或付费样本调用。
- 后续改善按 `docs/superpowers/specs/2026-09-07-selah-web-improvement-design.md` 与 `docs/superpowers/plans/2026-09-07-selah-web-improvement-plan.md` 审阅和分阶段开展；计划存在不代表其中的代码、数据库、配置或部署已经获准执行。

2026-09-08 实施授权更新：主人已明确要求「基于上面的内容完整开发」并继续任务，本轮可执行上述计划的 Web 代码、测试、已有种子音频的只读取回与本地打包、验收工具和候选构建。此授权取代本节此前仅限文档的阶段限制。保留原模型与声线。数据库 schema／migration、密钥、CI/CD、真实付费样本、远端部署和公开发布仍分别确认；先完成可审阅的本地实现与变更方案。可在工作区临时目录使用项目 CI 已指定并经官方校验的 Deno 二进制执行测试，不改变全局依赖或系统配置。

2026-09-09 执行状态更新：主人已确认临时使用 Supabase CLI；`speech-transcribe` 及四个生成／音频改善函数（`sentences-generate`、`sentences-prepare`、`sentences-batch-generate`、`audio-generate`）均已部署，五个函数的无凭证健康检查与远端源码哈希核对已完成。30 条例句的四种现有声线共 120 段已取回并打包，不重新调用 TTS。真实业务验收继续沿用已确认的调用预算，仍缺普通测试账户与中文录音样本；CLI 部署登录不能代替应用普通账户。数据库 schema 与 Web 公开发布仍按原有边界单独确认。

## 2026-09-10 循环听设计与规划边界

- 主人已认可「聆听」内新增「逐句听／循环听」的方案，并明确要求先完成 UI／UX 设计与整体开发计划，暂不修改代码。
- 本轮仅新增设计文档、静态界面设计稿和开发计划，并同步本规范与 `ROADMAP.md`；设计稿不进入应用资源或构建链路。
- 产品方向为当前账户完整未归档句库逐句双语循环，默认学习语言后母语；支持反向语序、15／30／60 分钟和自定义时长；循环播放独立于学习完成与成长奖励。
- 循环听产品代码、测试代码、音频生成／批量预制、数据库、依赖、配置与部署均未获本轮实施授权。既有 Web 改善授权不能自动扩展到循环听开发。
- 具体设计与实施分解分别见 `docs/superpowers/specs/2026-09-10-loop-listening-design.md` 与 `docs/superpowers/plans/2026-09-10-loop-listening-plan.md`；文档存在不代表功能已实现或通过真机验收。

## 2026-09-10 设置与繁体界面实施边界

- 主人已明确授权基于既有设计与实施计划完整开发 Web 设置、语言角色和本机能力；本阶段可实施产品代码、测试与本地构建，并同步路线图。
- 后续正式 Web 界面提供「繁體中文」「简体中文」「日本語」，默认 `zh-Hant`。首次使用、没有显式选择的旧用户和无有效本机偏好时均显示繁体，不依据浏览器语言改默认；用户明确选择后按本机保存并优先恢复。
- 母语可改为中文或日语，默认中文。中文母语的生成源语言代码仍是 `zh-Hant`；日语母语的新请求使用 `ja`。学习语言本期仍为英语。界面语言与母语互相独立。
- 切换界面只改变应用文案。切换母语只影响之后的输入、转写和生成，不得改写已有句子正文、语言元数据、音频缓存键或既有循环听会话。对用户开放日语母语生成前，须先验证服务端 `sourceLanguage=ja` 的 prompt／合约。
- 设置分为语言、陪伴、提醒、账户与同步、备份与离线、本机与应用；持久存储、安装和更新按真实状态呈现。安装不承诺防丢数据或后台提醒；更新保留本地保存、媒体与多窗口保护。
- 界面语言首期为本机偏好，不增加云端字段或数据库迁移。本阶段采用纯 Dart 文案表，不新增 `flutter_localizations`、`intl` 或字体依赖；`MaterialApp.locale` 仅负责系统控件的区域提示。
- 日语源语言 prompt 已纳入本次最小服务端改动与合约测试；循环听、后台、数据库 schema、密钥、CI、公开部署和真实发布仍不在本阶段授权内。
- 设计见 `docs/superpowers/specs/2026-09-10-settings-language-pwa-design.md`；两个实施计划分别为 `docs/superpowers/plans/2026-09-10-interface-language-plan.md` 与 `docs/superpowers/plans/2026-09-10-settings-pwa-plan.md`。

## 2026-09-10 会员、试用与费用保护规划边界

- 主人已认可「免费示例＋7 天限额试用＋39.9 元月会员」，本轮明确要求整理最终开发计划和 UI／UX 展示；授权范围为规范、设计、计划与独立展示稿，不实施产品、测试、数据库、支付、配置或部署。
- 试用权益为 30 条个人表达、3000 字符新增配音、5 分钟转写、3 次长文整理；月会员为 300 条、30000 字符、60 分钟、30 次。试用首次个人生成结果在服务器持久化成功后起算 168 小时；会员按服务端确认的付费账期恢复，不能按客户端时间或自然月首日重置。
- 用户界面不展示剩余额度、已用数量、消耗百分比或月底用完提醒；购买前说明完整额度和计量方式，在相应操作受限时提示原因、恢复日期及可继续使用的功能。
- 使用 OpenAI GPT 模型生成和整理学习内容的说明应出现在会员方案及生成入口；模型声明必须对应实际路由。配音另说明为 AI 合成，不承诺百分之百正确，不暗示人工审核或官方背书。
- 费用保护采用服务器统一入口、权益与费用原子预占、幂等与并发控制、未知费用保守占用、有限重试、平台总预算与基础设施保护。试用 2 元／会员每账期 20 元是待验证的模型费用预算目标，不是已实现的最高总账单保证；转写费用上界、完整权益可履约性、支付渠道与基础设施预算均为上线前验证门槛。
- 设计、计划与展示依据分别为 `docs/superpowers/specs/2026-09-10-membership-cost-control-design.md` 、 `docs/superpowers/plans/2026-09-10-membership-cost-control-plan.md` 与 `docs/superpowers/specs/2026-09-10-membership-cost-control-ui.html` 。新数据库 migration、密钥、依赖、支付真实交易、CI/CD、供应商配置和公开发布仍按原边界分别确认。

## 2026-09-10 笔记 B1＋B2 设计与规划边界

- 主人已认可笔记方案 B，并于 2026-09-10 明确要求按 B1＋B2（完整双语卡片＋轻量展开＋按词查看释义）计划完整实施；本轮允许修改正式 Web 笔记代码、对应测试、验收文档与路线图。
- 正式 Web「笔记」将取消截断英文和「选一句查看详情」。默认每张卡片显示完整中英、熟悉状态文字和一个真实词汇重点；拆解在原卡片展开，已有词汇可点开释义。允许同时展开多张卡片。
- 不恢复成长回忆入口；不新增笔记数据表、云端字段或词汇模型。听这句／练这句只跳转现有页面，不把笔记点击记为学习完成。
- B3 超长拆解备用层与 B4 按日期分组不进本期。既有循环听、会员、设置语言计划不因本文自动获得实施授权。
- 设计与计划见 `docs/superpowers/specs/2026-09-10-notes-b1b2-design.md` 与 `docs/superpowers/plans/2026-09-10-notes-b1b2-plan.md`；概念图不进入应用资源或构建链路。

## 2026-09-10 Stage 1 毛绒默剧 GIF 规划边界

- 主人选择方向一「一本正经，然后突然露馅」，并已授权执行 A01、A02、A03 的三个独立 GIF。
- 计划以当前三个 GIF 为形象和起止姿态基准；制作补绘了手势、眼神和重心变化的关键姿态，中间帧只做小幅衔接，不将整体旋转／伸缩当作动作本身。
- 计划见 `docs/superpowers/plans/2026-09-10-stage1-pantomime-gif-plan.md`；本轮使用内置图像生成能力补绘关键帧、本地 Pillow 清理透明边缘并编码，结果写入 `output/mascot-gif-stage1-pantomime-20260910-v1/`。
- 新版本已放入独立 HTML 供确认，未修改当前预览、其它 47 个 GIF 或应用资源；应用素材替换继续以主人另外明确允许为前提。

## 2026-09-10 Stage 1 默剧一致性修订

- 主人认可 A01、A02、A03 的动作内容，授权重新构思并生成，重点修正毛色跳变和不自然体态。
- 本轮采用固定身体母版与独立手脚图层，在本机合成三个动作；身体、叶片、材质、光照和基础比例固定，不逐帧重绘整只角色，不按每帧外接框缩放，也不对身体做挤压拉伸。
- 新输出位于 `output/mascot-gif-stage1-pantomime-20260910-v2/`。目录约定为 `raw/` 保存生成原图，`layers/` 保存不可变图层，`frames/` 保存派生帧，`individual/stage-1/` 保存三个 GIF，`storyboards/` 保存 4×4 图集；其余文件用于独立 HTML、制作说明及验证记录。
- 验证真实透明、无地面影子、至少 12％ 留白、每个动作 16 帧、首尾一致、统一调色板、固定身体比例，以及手脚连接和接地关系。旧版本、其它动作和应用素材保持原有状态，等待主人审阅。

## 2026-09-10 Stage 1 原图保真动作修订

- 主人已确认继续按「原图保真」方案修改上一版三个动作，目标是恢复正式 C 短绒角色的轮廓、五官层次、叶片颜色和原始纹理。
- 局部补绘试验版保留在 `output/mascot-gif-stage1-pantomime-20260910-v3/` 作为内部诊断；因移除手脚后的补洞会产生尖锐伪影，交付版改为直接复用正式运行 PNG 的完整姿态，不再使用生成身体 atlas 或按帧局部重绘。
- 交付版使用 `SelahFlutter/assets/sprites/PlushV4S1A01.png`—`A10.png` 中已批准的原始姿态，统一一次缩放和透明处理；动作只通过原始姿态切换、整只角色小幅位移和刚体倾斜表达，不改原始 PNG，不替换应用资源。
- 新输出位于 `output/mascot-gif-stage1-pantomime-20260910-v4-rigid3/`，包含三个独立 GIF、48 张 RGBA 帧、3 张 4×4 图集、来源清单和独立 `preview.html`；v2、v3 及此前预览均保留。

## 2026-09-11 剩余 47 个 GIF 动作设计边界

- 主人已认可 `8777` 的 Stage 1 A01—A03 原图姿态切换表现形式，本轮授权为沿用此方法设计剩余 47 个动作，准备后续 GIF 开发；不生成新的 GIF，不替换应用素材。
- 设计覆盖 Stage 1 A04—A10 共 7 个，以及 Stage 2—5 各 10 个。已有三个 GIF 保留为已认可的表现形式参考，所有动作继续一动作一文件。
- 每项设计写明原始业务动作、短剧情、同阶段来源姿态、16 帧顺序／时间／小幅刚体运动、首尾回归与特有验收点；禁止跨阶段拼接、生成新身体、拉伸／挤压躯干或独立摆动花叶。
- 规范与数据保存于 `docs/superpowers/specs/2026-09-11-mascot-47-action-design.md` 和同名 JSON；制作准备清单保存于 `docs/superpowers/plans/2026-09-11-mascot-47-gif-production-plan.md`。后续制作尚未执行。
- 独立设计总览目录为 `output/mascot-gif-47-design-20260911/`：`preview.html` 展示设计卡，`design.json` 是设计数据副本，`sources/` 保存未经修改的原始 PNG 副本，`approved/` 仅保存三个已有 GIF 的审阅副本；原始 PNG 预览中的地面影子不是未来 GIF 的交付效果。

## 完成定义

2026-09-07 Today 视觉修订：主人要求去掉吉祥物背景色块、精灵与欢迎文案真正居中、移除右上角重复小精灵，并明确选择本地脚本批量抠图、保留原图。运行素材导出真实 RGBA PNG，五阶段十动作与原始设计图保持对应；验证透明通道、轮廓与动作切换。Today 使用单一主精灵焦点，沿用既有 Quiet Growth 色彩和业务流程。

2026-09-06 新授权：主人已确认五阶段 V3 统一外形，明确要求生成五阶段各十动作共五十张图片，允许多个 agent 并行，并接入当前程序与网站。执行 `docs/superpowers/plans/2026-09-06-seed-plush-50-implementation.md`。本授权取代此前「只生成确认稿、不改运行代码」与必须固定旧 RGB 图集的限制；第一阶段保留十种已认可姿态意图，按最新外形生成正式状态图。允许按真实业务状态选择独立完整角色图并做短暂过渡与微动效，不能当作逐帧序列轮播；Reduce Motion 保持各阶段对应动作的静态姿态。旧稿保留。数据库、密钥、CI、外部部署边界仍不变。

任何事项只能按以下层级报告，不得混用：

1. **代码存在**：实现文件已提交。
2. **核心层已验证**：单元测试通过。
3. **产品已接线**：真实 App 运行路径使用该实现，不再依赖 Mock 或静态占位。
4. **平台已验证**：iOS 模拟器／真机或 Supabase 部署环境验证通过。
5. **发布完成**：TestFlight／App Store 流程实际完成。

只有达到对应层级，`ROADMAP.md` 才能标记相应状态。

## 实施顺序

1. 先更新规范和 `ROADMAP.md`。
2. 测试先行：先复现失败，再写最小修复。
3. 优先打通一条真实端到端闭环，再补外围能力。
4. 每阶段完成后运行相关测试；提交前运行完整 Swift、Supabase 和 iOS 构建验证。
5. 实现并验证后同步更新 `ROADMAP.md`。

## 工程约束

- 匹配现有 Swift／TypeScript 风格，不做无关重构。
- OpenAI、Supabase 密钥只存在于环境变量或服务端 secret，不进入 iOS、源码、日志或 commit。
- iOS 客户端不能直接持有 OpenAI API Key。
- Web 只允许公开的 Supabase URL 与 publishable／anon key；OpenAI、service-role 与 VAPID 私钥只能在服务端。已有依赖与浏览器标准 API 优先，不新增全局依赖。
- Web 本地数据按账户隔离，IndexedDB 写入失败必须可见；备份导入先完整校验，再原子合并。云端同步沿用现有表与 RLS，不能伪造同步成功。
- Web 使用真实 seed 内容作为未登录时的本地学习资料；生成、整理、转写必须通过真实服务，缺少配置或权限时明确反馈。播放完成事件只由真实音频结束触发。
- Web 保持暖米色、珊瑚色的 Quiet Growth 品牌和已选 C 短绒精灵；角色按最新授权的五阶段五十状态素材精确选图，不将独立姿态当序列帧轮播。在宽屏使用侧栏，在手机使用底部导航，支持键盘、可访问名称和 Reduce Motion；一次性反馈不循环，后台停止角色动画。
- 产品运行路径不得静默退回 Mock；Mock 仅用于测试和 Preview。
- Seed 导入必须幂等；后台任务必须可中断、可恢复。
- 事件 metadata 使用白名单，禁止上传原始句子等自由文本。
- 新增数据库迁移、依赖、CI 配置、`.env` 或外部部署前必须取得主人确认。

## 验证命令

```bash
swift package resolve
swift build
swift test
deno test --allow-env --allow-read supabase/tests
```

iOS 工程建立后还必须执行项目定义的 `xcodebuild` 模拟器构建与测试命令。Windows 本机没有 Swift／Deno／Xcode 时，必须使用 macOS CI 或明确说明未验证，不能推断成功。

Web 修改运行 `flutter analyze`、`flutter test`、`flutter build web --release`，并在实际浏览器验证学习、持久化、离线、音频和响应式布局。原生 Swift 未改动时无需为 Web 交付重新声称其平台验证结果；服务端改动另跑对应 Deno 测试。线上服务、iPhone Safari 与生产部署必须分别报告实际验收状态。

## 文档职责

- `README.md`：项目介绍、真实工程状态和运行方式。
- `ROADMAP.md`：当前阶段、已完成、进行中、阻塞、最近验证。
- `docs/`：设计决策、隐私边界和发布验收证据。
