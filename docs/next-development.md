# Selah 后续开发清单

更新日期：2026-09-15。本页是后续任务入口；真实进度以 [ROADMAP](../ROADMAP.md) 为准。当前保持原语音与文本模型；管理员用量统计、五类供应商 attempt、活动心跳、管理台页面和 OpenAI 每日费用同步入口已完成本地实现与测试，实施记录见 [管理员用量与费用统计](admin-usage-dashboard-2026-09-15.md)。OpenAI 普通密钥、Admin Key 与 Project ID 已在本地验证通过；第一批远端上线仍需恢复有效 Supabase 登录或凭据，然后只执行 `005` 统计 migration、管理员授权、服务端 Secrets、两个管理函数和五个统计接入函数部署，不能用整包 `db push` 顺带发布尚未单独验收的 `006` 会员迁移。

## 当前开发入口

- [费用调研档案](2026-09-07-web-ai-cost-research.md) ：保存官方价格、替代方案、测算假设与“维持原方案”的决定。
- [逐项改善设计](superpowers/specs/2026-09-07-selah-web-improvement-design.md) ：13 项“原本怎么样／修改后怎么样／怎样验收”。
- [整体开发计划](superpowers/plans/2026-09-07-selah-web-improvement-plan.md) ：任务依赖、具体文件、验证方式与执行边界。
- [首装性能验收](web-performance-2026-09-09.md) ：预缓存减少约 16.67 MB，两种 CanvasKit 的离线启动与全部 120 段音频通过桌面浏览器验证。

| 优先级 | 剩余工作 | 开始所需条件 | 完成标准 |
| --- | --- | --- | --- |
| P0 | T09：真实转写、单句／批量生成与 TTS | 普通测试账号、一段中文录音；沿用已确认调用预算，执行时显式启用计费开关 | 真实响应、重放、MP3 下载与音质逐项有记录；执行入口见 [真实远端验收说明](remote-acceptance.md) |
| P0 | T09：账户隔离与跨设备保存 | 第二个普通账号、第一账号的私人句子／事件样本、两台设备 | 账户之间不可读写私有数据；同一账号在两台设备恢复聆听、练习、笔记和复习状态；脚本只读隔离检查之外的写入／UI 检查仍需执行 |
| P0 | T09：真实转写、单句／批量生成与 TTS | 管理统计远端已上线；五个收费函数已重新部署并接入 attempt 记录，但尚未用普通账号产生真实供应商调用 | 普通测试账号、一段中文录音和计费开关就绪后，按 [真实远端验收说明](remote-acceptance.md) 执行；验证真实响应、重放、MP3 下载、attempt 记录和 Dashboard 汇总 |
| P1 | T06：真实用量与费用估算 | 远端只应用并登记 `005`，管理员与两个管理函数已上线；`006` 会员迁移确认未应用；OpenAI 最近 7 天费用同步成功但金额为 0 | 等 T09 产生真实调用后，核对业务请求、供应商调用、估算费用、OpenAI 实际费用和普通账号权限 |
| 发布前 | T10：真机、PWA 与性能 | 实际 iPhone、Android、桌面浏览器及可用 HTTPS 测试入口 | 麦克风、软键盘、后台恢复、离线音频、安装更新和弱网加载有设备／版本／Build ID 记录；手机视口模拟只作为布局证据 |
| 发布前 | T11：公开上线 | 前述发布必需验收通过，确定托管目标和独立发布确认 | 同一候选在目标 HTTPS 地址运行，更新／缓存与生产冒烟通过；当前本地 Build ID 为 `1c0036e1c11f90a9` |
| 后续独立增强 | T12：关闭网页后的后台提醒 | 明确需要后台提醒，再确认订阅表、VAPID 与调度配置 | 真机在网页关闭后收到正确且不重复的提醒；当前暂缓 |

当前提供的 CLI 登录用于后端部署；本执行进程尚无应用普通测试账号与录音路径。其余待确认事项分别是数据库 schema 和 Web 公开发布，依据根目录 `CLAUDE.md` 的执行边界。已批准的五个函数部署不需要重复确认或重复执行。

以下清单保留角色交付与设备事项的上下文；已更新被后续决定替代的素材要求，具体实施顺序以上述计划为准。2026-09-06 的 79 项 Flutter、17 项 Node 与构建结果为历史证据，不代表本轮新任务已验证。

## 已确认的当前方向

- 最新优先级（2026-09-08）：五阶段五十动作与真实 RGBA 运行素材已完成；先修输入与分段、恢复真实转写并验收普通用户链路，素材进一步优化由实测决定。
- 当前角色采用 C「短绒织物」，设计包位于 `design-system/selah/mascot/current-c-design-pack.md`。
- D「磨砂琥珀」保留作后续其他吉祥物候选，本阶段不开发。
- Web 的学习页面、本地存储、复习、笔记、同步客户端、备份、PWA、页面输入持久化、20 段分批、相同句子复用和 120 段四声线音频已有本地验收记录；C 已在正式 Web 页面运行。后续重点是真实服务、用量统计与设备验收。

## 先完成：当前 Web 与 C 角色

| 顺序 | 工作 | 当前证据与缺口 | 完成条件 |
| --- | --- | --- | --- |
| 1 | C 发布性能实测 | 2026-09-07 已按主人选择保留原图并导出真实 RGBA，50 张运行素材合计约 21.74 MiB；当前首装预缓存 14 张，不等于全部素材体积 | 测量首屏、阶段切换、内存与低速网络；按瓶颈决定多尺寸或进一步压缩，透明制作不再重复安排 |
| 2 | 五阶段五十动作接入 | 已完成：正式 Web、原生 Flutter 展示、Gallery、Reduce Motion、缓存与两个 Release 构建通过 | 发布候选素材替换后复跑自动化和设备视觉验收 |
| 3 | 十动作及业务触发 | 已实施并通过自动化与本地浏览器验证：准备、播放、自然结束、录音／转写、复习及闲置；一次反馈后归位，Reduce Motion／后台停止 | 真实录音与云端转写效果仍随第 7 项联调；测试替身不代表服务已上线 |
| 4 | 圆形悬浮开始按钮 | 已实施并验证：64px 圆形、已选数量、姓名与恰好三句校验、忙碌禁用、键盘；错误提示固定在按钮上方 | 窄屏与滚动已验证，实体手机软键盘随设备验收完成 |
| 5 | 后端连接状态 | 2026-09-06 域名和认证入口已恢复，只读精确计数通过 | 保留可达性监控；这项结果不能替代普通用户权限和业务调用验收 |
| 6 | 真实服务端到端验收 | 认证、生成、TTS、同步客户端已有本地合约验证，真实远端仍未验收 | 用测试账户完成登录、中文输入、英文生成、TTS、聆听、复习、跨设备同步，以及超时、重试、额度和账户切换错误路径 |
| 7 | 真实语音转写 | 浏览器 MediaRecorder、`speech-transcribe` 源码、本地函数注册和远端单函数部署已完成；零费用检查为 `OPTIONS 200`、无凭证 `POST 401`，真实音频链路未完成 | 使用普通账户与实体麦克风验证录音、权限拒绝、取消、转写、幂等重放和资源释放 |
| 8 | 种子音频补齐 | 已从现有远端 ready 清单取回并打包 30 句 × 4 声线共 120 段；Chrome 断网读取、哈希和四声线抽样解码通过，新增 TTS 为 0 | 真机存储清理、低速网络和安装 PWA 后随 T10 复验；不重复生成已有音频 |

下一阶段在既定预算内完成普通账户真实业务验收；用量统计 schema 与真机矩阵分别推进。2026-09-09 转写函数和四个生成／音频函数已完成远端部署，零费用检查均为 `OPTIONS 200`、无凭证 `POST 401`，本地替身测试和网关检查不能替代真实账户线上结果。

## 发布前：验证与交付

| 工作 | 剩余内容 | 完成条件 |
| --- | --- | --- |
| 真实设备与性能 | iPhone Safari、Android 浏览器、实体麦克风、软键盘、触控、窄屏、低速网络、PWA 安装和更新 | C 的大小与动画不妨碍学习；页面状态、权限、播放与存储在真实设备通过；手机视口模拟不能代替真机 |
| 回归与离线 | 本轮本地回归已完成，新角色可从缓存加载，断网重开并播放内置音频后记录仍能保存 | 发布候选或素材再次变更时复验；实体设备存储清理、低速网络和安装更新仍待完成 |
| HTTPS 发布准备 | 托管环境、域名与必要配置、发布说明、隐私政策及真实环境验收 | 先准备可审阅的构建和部署方案，再按项目规则执行外部发布；上线后分别记录服务与设备结果 |

## 后续增强与保留客户端

| 工作 | 当前缺口与进入条件 |
| --- | --- |
| 关闭网页后的后台提醒 | Web Push 接收与适配已有代码，订阅持久化、发送器、VAPID 和调度尚未实施；需要后台提醒时按 `docs/web-push-plan.md` 开发 |
| 旧 iOS 历史数据迁入 Web | 需要 SwiftData 专用导出及迁入验收；Web JSON 备份功能不能替代旧设备导出。有旧历史数据需要完整迁移时必须完成 |
| 保留的原生客户端 | 共享角色组件与 Gallery 已支持 50 张，但旧原生页面仍有固定阶段，未实现按学习事件成长；需补齐真实阶段接线、服务替换、macOS CI、iOS／Android 真机语音音频、Swift 真机视觉与 TestFlight／商店资料；与 Web 完成度分开记录 |
| D 磨砂琥珀及其他吉祥物 | 后续明确开发其他角色时，再以 `seed-v4-d-frosted-amber.png` 和相关探索记录展开；本阶段只保留，不排入 C 的交付范围 |
| 五阶段各十动作 | 50 张设计与运行素材、统一映射、过渡、缓存和 Reduce Motion 已完成本地验收；发布前仍需素材优化及真机性能复验 |

## 执行边界

完成项仅代表明确记录的本地实现与验证。涉及真实环境的 schema、密钥、CI、外部配置及公开发布时，仍按既有项目规则执行；本轮未改变这些配置。

## 本轮核对依据

- `SelahFlutter/lib/features/companion/plush_companion_poses.dart`、`SelahFlutter/lib/web/ui/plush_companion.dart`、`SelahFlutter/lib/features/companion/selah_sprite.dart`：五阶段五十姿态共用映射、按阶段预热、完整图片与微动效。
- `SelahFlutter/lib/domain/selah_enums.dart`：十个动作及五个成长阶段枚举。
- `SelahFlutter/lib/web/learning_controller.dart`、`ui/web_learning_app.dart`、`ui/web_start_action.dart`：真实事件、引导页固定操作与错误提示。
- `docs/web-plush-50-acceptance.md`：本轮素材、测试、构建和浏览器证据；`docs/web-plush-acceptance.md` 与 `docs/web-acceptance.md` 保留此前基线。
- `docs/web-speech-service.md`、`docs/web-push-plan.md`：服务源码完成度与未执行的部署／数据方案。
- `ROADMAP.md` 的 2026-09-06 UI／UX 只读验收记录：后端域名与认证入口恢复，30 条 seed、120 条 ready 音频清单；真实业务闭环仍待验收。
